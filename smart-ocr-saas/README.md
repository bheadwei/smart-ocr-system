# Smart OCR SaaS

雲端智能文字辨識服務 - 基於 OpenAI Vision API 的企業級 OCR 解決方案

## 開發進度

```
整體完成度: ████████░░░░░░░░░░░░ 40%

Phase 1: 基礎架構   [████████████████████] 100% ✅
Phase 2: 認證系統   [████████████████░░░░]  80% 🔄
Phase 3: OCR 核心   [████████████░░░░░░░░]  60% 🔄
Phase 4: 匯出歷史   [████████░░░░░░░░░░░░]  40% 🔄
Phase 5: 優化上線   [░░░░░░░░░░░░░░░░░░░░]   0% ⏳
```

## 技術棧

### 前端
- **框架**: Next.js 14 (App Router)
- **UI**: TailwindCSS + Radix UI
- **狀態管理**: Zustand
- **語言**: TypeScript

### 後端
- **框架**: Python FastAPI
- **資料庫**: MongoDB (Motor async driver)
- **物件儲存**: MinIO (S3 相容)
- **快取**: Redis
- **AI 服務**: OpenAI Vision API (GPT-4V)
- **PDF 處理**: PyMuPDF

### 基礎設施
- **容器化**: Docker + Docker Compose
- **認證**: JWT + LDAP/AD

## 功能特點

- ✅ 本地帳號登入
- 🔄 LDAP/AD 企業登入 (開發中)
- ✅ 圖片辨識 (JPG, PNG, WEBP, etc.)
- ✅ PDF 文件辨識 (最多 10 頁)
- 🔄 WebSocket 即時處理進度 (開發中)
- ✅ JSON 匯出
- 🔄 CSV/XLSX 匯出 (開發中)
- ✅ 歷史紀錄管理

## 快速開始

### 1. 環境需求

- Docker & Docker Compose
- Node.js 20+ (本地開發)
- Python 3.10+ (本地開發)
- OpenAI API Key

### 2. 設定環境變數

```bash
# 複製環境變數範例檔
cp .env.example .env

# 編輯 .env 並填入 OpenAI API Key
# OPENAI_API_KEY=sk-your-key-here
```

### 3. 啟動服務 (Docker Compose)

```bash
# 生產模式
docker-compose up -d

# 或開發模式 (支援熱更新)
docker-compose -f docker-compose.dev.yml up -d
```

### 4. 存取服務

| 服務 | URL | 說明 |
|------|-----|------|
| 前端 | http://localhost:3000 | Web 介面 |
| 後端 API | http://localhost:8000/api/v1/docs | Swagger 文件 |
| MinIO Console | http://localhost:9001 | 物件儲存管理 |

> MinIO 預設帳密: `minioadmin` / `minioadmin`

## 本地開發

### Backend

```bash
cd backend

# 建立虛擬環境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安裝依賴
pip install -r requirements.txt

# 複製環境變數
cp .env.example .env

# 啟動開發伺服器
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend

```bash
cd frontend

# 安裝依賴
npm install

# 複製環境變數
cp .env.example .env.local

# 啟動開發伺服器
npm run dev
```

## 專案結構

```
smart-ocr-saas/
├── backend/                 # FastAPI 後端
│   ├── app/
│   │   ├── main.py          # 應用程式入口
│   │   ├── config.py        # 配置管理
│   │   ├── api/v1/
│   │   │   ├── router.py    # API 路由
│   │   │   └── routes/
│   │   │       ├── auth.py      # 認證 API
│   │   │       ├── admin.py     # 管理 API
│   │   │       ├── ocr.py       # OCR API
│   │   │       ├── export.py    # 匯出 API
│   │   │       ├── history.py   # 歷史 API
│   │   │       └── websocket.py # WebSocket
│   │   ├── core/
│   │   │   ├── database.py      # MongoDB 連線
│   │   │   ├── dependencies.py  # 依賴注入
│   │   │   └── exceptions.py    # 例外處理
│   │   ├── models/
│   │   │   ├── user.py          # 使用者模型
│   │   │   └── ocr.py           # OCR 模型
│   │   ├── services/
│   │   │   ├── auth_service.py  # 認證服務
│   │   │   ├── ldap_service.py  # LDAP 服務
│   │   │   ├── user_service.py  # 使用者服務
│   │   │   ├── ocr_service.py   # OCR 服務
│   │   │   ├── file_service.py  # 檔案服務
│   │   │   └── websocket_service.py
│   │   └── repositories/
│   │       ├── user_repository.py
│   │       ├── ocr_task_repository.py
│   │       └── ocr_result_repository.py
│   ├── tests/
│   ├── Dockerfile
│   ├── Dockerfile.dev
│   └── requirements.txt
│
├── frontend/                # Next.js 前端
│   ├── app/
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   ├── page.tsx         # 首頁
│   │   ├── (auth)/
│   │   │   └── login/       # 登入頁
│   │   └── (dashboard)/
│   │       ├── layout.tsx   # Dashboard 布局
│   │       └── history/     # 歷史紀錄頁
│   ├── stores/
│   │   └── authStore.ts     # 認證狀態
│   ├── hooks/
│   │   └── useWebSocket.ts  # WebSocket Hook
│   ├── lib/
│   │   └── utils.ts         # 工具函數
│   ├── types/
│   │   └── index.ts         # TypeScript 型別
│   ├── Dockerfile
│   ├── Dockerfile.dev
│   └── package.json
│
├── docs/                    # 文件 (位於父目錄)
│   ├── smart_ocr_saas_prd.md           # 產品需求
│   ├── smart_ocr_saas_architecture.md  # 架構設計
│   ├── smart_ocr_saas_api_spec.md      # API 規格
│   └── smart_ocr_saas_wbs.md           # 開發計畫
│
├── docker-compose.yml       # 生產環境配置
├── docker-compose.dev.yml   # 開發環境配置
├── .env.example             # 環境變數範例
├── .gitignore
└── README.md
```

## Docker 服務架構

```
┌─────────────────────────────────────────────────────────┐
│                     Docker Network                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────┐    ┌──────────┐    ┌──────────────────┐  │
│  │ Frontend │───▶│ Backend  │───▶│ OpenAI Vision API│  │
│  │ :3000    │    │ :8000    │    │ (External)       │  │
│  └──────────┘    └────┬─────┘    └──────────────────┘  │
│                       │                                  │
│         ┌─────────────┼─────────────┐                   │
│         ▼             ▼             ▼                   │
│    ┌─────────┐  ┌──────────┐  ┌─────────┐              │
│    │ MongoDB │  │  MinIO   │  │  Redis  │              │
│    │ :27017  │  │ :9000/01 │  │  :6379  │              │
│    └─────────┘  └──────────┘  └─────────┘              │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## API 文件

啟動後端後，存取 Swagger UI：
- http://localhost:8000/api/v1/docs

### 主要 API 端點

| 方法 | 路徑 | 說明 |
|------|------|------|
| POST | `/api/v1/auth/login` | 使用者登入 |
| POST | `/api/v1/auth/logout` | 使用者登出 |
| POST | `/api/v1/ocr/upload` | 上傳檔案 |
| GET | `/api/v1/ocr/tasks/{id}` | 取得任務狀態 |
| GET | `/api/v1/ocr/results/{id}` | 取得辨識結果 |
| GET | `/api/v1/history` | 歷史紀錄列表 |
| GET | `/api/v1/export/{id}` | 匯出結果 |

## 相關文件

- [PRD 文件](../docs/smart_ocr_saas_prd.md)
- [架構設計文件](../docs/smart_ocr_saas_architecture.md)
- [API 規格文件](../docs/smart_ocr_saas_api_spec.md)
- [WBS 開發計畫](../docs/smart_ocr_saas_wbs.md)
- [專案狀態](../docs/smart_ocr_saas_project_status.md)

## License

MIT
